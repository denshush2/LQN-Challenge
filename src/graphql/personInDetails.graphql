query PersonInDetails($id: ID!) {
  person(id: $id) {
    filmConnection {
      totalCount
      films {
        title
        planetConnection {
          totalCount
          planets {
            name
          }
        }
      }
    }
  }
}
